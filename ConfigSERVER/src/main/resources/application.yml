

server:
  port: 8090



spring:
  cloud:
   config:
     server:
      git:
       uri: http://localhost:8761

       health:
         enabled: true
   name: config-server

  management :
    endpoint:
      health:
        show-details: always

